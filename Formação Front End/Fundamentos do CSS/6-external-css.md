# External CSS

- Para adicionar CSS com esta técnica precisamos criar um arquivo .css;
- Geralmente eles ficam numa pasta chamada css;
- E nós importamos através da tag link;
- As regras que estão no arquivo são aplicadas no HTML;